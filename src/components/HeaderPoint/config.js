export const config = {
    multiple: true,
    filterable: false,
    searchable: false,
    "close-on-select": false,
    reduce: (option) => option.id,
    placeholder: "Выберите пункт проката",
};
